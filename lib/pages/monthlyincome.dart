import 'package:flutter/material.dart';

class MonthlyIncome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Monthly Income'),
      ),
      body: Center(
        child: Text('This is the Monthly Income page'),
      ),
    );
  }
}

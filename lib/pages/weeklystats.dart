import 'package:flutter/material.dart';

class WeeklyStats extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Weekly Stats'),
      ),
      body: Center(
        child: Text('Pie Chart/Bar Chart Here!'),
      ),
    );
  }
}

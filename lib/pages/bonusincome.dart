import 'package:flutter/material.dart';

class BonusIncome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bonus Income'),
      ),
      body: Center(
        child: Text('This is the Bonus Income page'),
      ),
    );
  }
}